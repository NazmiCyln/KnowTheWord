<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/splash4"
    tools:context=".SplashScreenActivity">

    <ImageView
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:src="@drawable/name1"
        tools:ignore="MissingConstraints"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="30dp"/>

    <ProgressBar
        android:id="@+id/splashActivity_progressBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:ignore="MissingConstraints"
        android:padding="20dp"
        android:min="0"
        android:max="100"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="120dp"/>

    <TextView
        android:id="@+id/splashActivity_txtViewState"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/splashActivity_progressBar"
        app:layout_constraintLeft_toLeftOf="@+id/splashActivity_progressBar"
        app:layout_constraintRight_toRightOf="@+id/splashActivity_progressBar"
        android:padding="5dp"
        tools:ignore="MissingConstraints"
        android:textSize="17sp"
        android:textColor="#55380B"
        android:text="Yükleniyor..."/>


</androidx.constraintlayout.widget.ConstraintLayout>